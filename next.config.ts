import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['cdn.discordapp.com', 'skillicons.dev', 'i.pinimg.com', 'i.scdn.co', 'raw.githubusercontent.com', 'cdn.iconscout.com'],
  },
};

export default nextConfig;
